name: Deploy Documentation

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -l {0}
    env:
      # slv resolves the CHPC data roots at import time; nothing in CI reads
      # them, so point them at throwaway paths so the modules import.
      LINGROUP_DATA_DIR: /tmp/slv-data/group
      SLV_USER_DATA_DIR: /tmp/slv-data/user
      SLV_DAQ_DIR: /tmp/slv-data/daq
      SLV_SOUNDINGS_DIR: /tmp/slv-data/soundings

    steps:
      - uses: actions/checkout@v5

      - name: Create conda environment
        uses: mamba-org/setup-micromamba@v2
        with:
          cache-downloads: true
          micromamba-version: "latest"
          environment-file: ci/environment.yml

      - name: Install slv (editable)
        run: pip install --no-deps -e .

      - name: Setup just
        uses: extractions/setup-just@v3

      - name: Build documentation
        run: |
          just build-docs

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/_build/html

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
